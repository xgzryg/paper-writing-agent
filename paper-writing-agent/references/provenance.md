# 组件与许可说明

本发行目录包含运行所需的 agent/skill 指令、脚本、模板和共享资源。主入口通过相对路径读取这些组件；功能列表与数量见包根的 `bundle-manifest.json`，运行条件见[依赖说明](dependencies.md)。

## 第三方组件

组件文件中已有的作者署名、版权与许可声明继续保留并适用。公开发行版本没有为这些组件另行授予统一许可证，也不把未注明许可证的材料自动解释为可任意再分发。

学术文风支持组件包含 Copyright (c) 2025 redbaronyyyyy-eng 的 MIT 许可文件，完整条款保留在[对应 LICENSE](../skills/wordpolish-academic/skills/humanizer-zh-academic/LICENSE)。其他组件的具体声明以各自文件中的内容为准。模板或组件作者不因此成为用户所撰写论文或生成报告的作者。

## 运行范围

运行不依赖个人课程归档、构建记录或其他机器上的安装目录。审稿和返修以本次实际取得的材料为依据，不声称已读取未提供的外部案例库。

期刊指标本地查询随包包含带年份的 `2025IF.xlsx` 和 `journals_index.json`，并允许用户以 `--data-file` 指定其有权使用的替代索引。数据年份、数据来源与当前核验状态须分开记录；内置快照不应被表述为实时指标。联网服务与全文内容的实际访问条件不因安装本包而改变。

组件中的示例和历史说明不会覆盖当前宿主、用户授权与[共享规则](shared-rules.md)。关于路由与边界的实现约定见[分支执行说明](source-skill-adaptations.md)。
