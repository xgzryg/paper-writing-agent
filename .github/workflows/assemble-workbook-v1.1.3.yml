name: Finalize v1.1.3 workbook asset

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  finalize:
    if: "!contains(github.event.head_commit.message, 'Finalize v1.1.3 workbook asset')"
    runs-on: ubuntu-latest
    steps:
      - name: Check out release tree
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Restore and verify workbook
        run: python .github/assemble_workbook_v1_1_3.py
      - name: Commit complete workbook
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "Finalize v1.1.3 workbook asset [skip ci]"
          git push
